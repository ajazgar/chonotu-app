doctype html
html(lang='pl')
  head
    meta(charset='utf-8')
    meta(name='viewport', content='width=device-width, initial-scale-1')
    link(rel='stylesheet', href='/stylesheets/style.css')
    title Moje konto
  body
    script.
      function redirectToDelete() {
      var login = location.search.split('login=')[1];
      var url= '/delete?userToDelete=' + login;
      window.location.replace(url);
      }
    nav
      .inner-topnav
        a.button(href='/')
          img(src='', alt='LOGO')
        .topnav-buttons
          a.button(href='/zarejestruj') Zarejestruj się
          a.button(href='/zaloguj') Zaloguj się
          a.button(href='/about') O aplikacji
          a.button(href='/kontakt') Kontakt
    main
      .user-info
        h3 Nazwa użytkownika
        ul
          li Data urodzenia
          li Mail
          li Telefon
      .content-buttons
        button.button(onclick='redirectToDelete()') Usuń konto
        .button Edytuj dane
      .user-tickets
        h2 Zakupione bilety
        img(src='', alt='event-image')
        .event-info
          h2 Nazwa wydarzenia
          p Ilość biletów:
          p Cena:
      .user-tickets
        img(src='', alt='event-image')
        .event-info
          h2 Nazwa wydarzenia
          p Ilość biletów:
          p Cena:
      .user-tickets
        img(src='', alt='event-image')
        .event-info
          h2 Nazwa wydarzenia
          p Ilość biletów:
          p Cena:
    footer
      p FOOTER

